<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.edu.module.dao.ArchiveAssessDao">

    <resultMap type="net.edu.module.entity.ArchiveAssessEntity" id="archiveAssessMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="grade" column="grade"/>
        <result property="deleted" column="deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="creator" column="creator"/>
        <result property="version" column="version"/>
        <result property="updater" column="updater"/>
    </resultMap>

    <select id="selectArchiveAssessByPage" resultMap="archiveAssessMap">
        select
            id,
            name,
            grade
        from archive_assess
        where deleted = 0
        <if test="query.getName()!=null and query.getName()!=''">
            and name like concat('%',#{query.name},'%')
        </if>
        <if test="query.getGrade()!=null and query.getGrade()!=''">
            and grade = #{query.grade}
        </if>
    </select>
</mapper>