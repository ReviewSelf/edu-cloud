<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.edu.module.dao.AbilityDao">


    <select id="selectAbilityItemList" resultType="net.edu.module.vo.AbilityVO">
        select id,pid,name,level,creator,(select name from ability where id=#{pid})as parentName
        FROM ability WHERE deleted=0 AND pid=#{pid}
    </select>
</mapper>